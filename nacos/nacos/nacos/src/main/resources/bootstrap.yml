server:
  port: 8999

spring:
    application:
        name: nacos-test
    cloud:
        nacos:
          discovery:
                enabled: true
                server-addr: localhost:8848
                ip: localhost
          config:
                enabled: true
                server-addr: localhost:8848
                file-extension: yaml
